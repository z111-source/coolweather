pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()   //加上后litePal可用了
        maven {url'https://jitpack.io'}  //同上
    }
}
rootProject.name = "CoolWeather"
include ':app'
include ':networkTest'
include ':fragmenttest'
